const detectInstalled = require('detect-installed');

module.exports = function maybeLoadSharp() {
  return detectInstalled('sharp', { local: true })
    .then(installed => {
      if (installed) {
        return require('sharp');
      } else {
        return null;
      }
    })
}
